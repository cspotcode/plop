on: ['push', 'pull_request']
jobs:
  continuous:
    name: Continous
    steps:
      - name: git clone
        uses: actions/checkout@v2
      - name: test
        uses: cspotcode/docker-images/images/node-ci@master
        run: echo foobar